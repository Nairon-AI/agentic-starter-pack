# Catalog

This is the human-browsable category map for the repo.

- `skills/` is the installable source of truth used by `npx skills add`
- `catalog/` groups the same skills by theme so the repo is easier to scan
- entries inside `catalog/` are aliases that point back to the real skill folders in `skills/`
