---
name: verify
description: Prove a completed engineering task works by exercising the app, collecting evidence, running regression checks, and preparing review-ready proof. Use before opening or handing off a PR, after implementing a feature or bug fix, or when the user asks to verify work end to end.
user-invocable: true
---

# Verify

Goal: prove this task works before handoff.

Generated by `verifier-setup` on `{{DATE}}`.

## Repo Facts

- Stack up: `{{STACK_UP}}`
- App URL: `{{APP_URL}}`
- Run mode: `{{RUN_MODE}}`
- Driver: `{{DRIVER}}`
- Auth helper: `{{AUTH_HELPER}}`
- Regression checks: `{{REGRESSION_CMDS}}`
- Evidence upload: `{{EVIDENCE_UPLOAD}}`

{{RUN_MODE_NOTE}}

## Workflow

1. Confirm the branch has the task changes committed.
2. Start or attach to the app with `{{STACK_UP}}`.
3. Prepare auth with `{{AUTH_HELPER}}` when needed.
4. Exercise the changed behavior:

   ```text
   {{EXERCISE}}
   ```

5. Capture evidence: screenshots, video, logs, command output, or API responses as appropriate.
6. Run regression checks:

   ```sh
   {{REGRESSION_CMDS}}
   ```

7. Upload or save evidence using:

   ```sh
   {{EVIDENCE_UPLOAD}}
   ```

8. Hand off with:
   - What was verified
   - Evidence links or files
   - Checks run and results
   - Remaining gaps or risks

## Driver Notes

{{DRIVER_INSTRUCTION}}

## Auth Notes

{{AUTH_INSTRUCTION}}
