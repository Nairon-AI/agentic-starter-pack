Vendored from `https://github.com/remorses/zeke`.

Purpose:
- keep the installer CLI self-contained
- avoid extra dependency fetching at runtime
- preserve a stable build surface for this repo's launcher
